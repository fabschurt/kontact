{% if name is defined %}
  {{- 'kontact.label.name'|trans }}: {{ name }}

{% endif %}
{% if address is defined %}
  {{- 'kontact.label.address'|trans }}: {{ address }}

{% endif %}
{{ 'kontact.label.message'|trans }}:

{{ message }}
